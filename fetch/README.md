# Fetch Workspace

This directory is intentionally isolated from the Preview ID core.

Current state:
- `/fetch/` route exists.
- Root navigation patch will link here.
- Existing Fetch/CodeHunt engine should be restored here after identifying its canonical source.
